import javax.swing.*;
import java.awt.*;

/*public class MoveSlider extends JFrame {
    private JButton confirm;
    private JSlider deploy;
    public MoveSlider(int min, int max, Game model, GameView view)
    {
        super();
        //slider setting
        deploy = new JSlider(JSlider.HORIZONTAL,min,max,max);
        deploy.setMajorTickSpacing(1);
        deploy.setPaintLabels(true);
        add(deploy);
        //confirm Button
        confirm = new JButton("OK");
        add(confirm);
        setLayout(new FlowLayout());
        pack();
        setVisible(true);
        view.setEnabled(false);
        model.setUnFinishedDeploy(true);
    }

    public JButton getConfirm() {
        return confirm;
    }

    public int getValue()
    {
        return deploy.getValue();
    }
}
*/