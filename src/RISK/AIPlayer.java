package RISK;

public class AIPlayer extends Player{
    /**
     * Instantiates a new Player.
     *
     * @param name the player's name
     * @param AI   the ai
     */
    public AIPlayer(String name, boolean AI) {
        super(name, AI);
    }
}
